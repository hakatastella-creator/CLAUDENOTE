# アライナー チェアタイム計算

矯正のアライナーが届いたときに、次回来院の所要時間を出して、患者さんへの連絡文まで作るツールです。
`index.html` を開くだけで動きます（インストール不要。Artifact として公開した場合は設定を医院内で共有できます）。

## 使い方

1. クリンチェックを見て入力する
   - アタッチメント除去：個数
   - アタッチメントセット：口腔マップ（6分割）でブロックを選択
   - ボタン除去：口腔マップでブロックを選択
   - ボタンセット：個数
   - IPR：か所数
2. 上部に「予約枠」が出る
3. 患者さんのお名前を入れて「本文をコピー」→ LINE やメールに貼り付け

## 時間の計算

口腔を6ブロック（上下 × 右臼歯・前歯・左臼歯）に分けて考えます。

| 処置 | 数え方 | 初期値 |
| --- | --- | --- |
| アタッチメント除去 | 個数で段階 | 1〜4個：15分／5個以上：30分 |
| アタッチメントセット | ブロック | 7.5分／1ブロック（臼歯のみ4ブロック=30分・全体6ブロック=45分） |
| ボタン除去 | ブロック | 7.5分／1ブロック |
| ボタンセット | 個数 | 5分／1個 |
| IPR | か所 | 3分／1か所（仮の値。実際に合わせて変更してください） |
| 基本（お渡し・チェック・説明） | 固定 | 10分 |

```
所要時間 = 基本時間 + アタッチメント除去 + 各ブロック合計 + 個数×単価
予約枠   = 所要時間を丸め単位（初期値5分）で切り上げ、最短の予約枠（初期値15分）を下回らない
```

すべての値は画面の「時間の設定」から変更できます。

## 設定の保存先

- Artifact として公開している場合: 保存するとスタッフ全員に共有されます（`db` 機能）
- ファイルを直接開いている場合: その端末のブラウザにのみ保存されます
