# CLAUDE.md

このリポジトリで作業するときの共通ルール。

## LINE送信の指示文に関する共通ルール（必須）

LINE送信に関する指示文（例：`LINEリマインド指示文.md`）を新規作成・編集するときは、
**今後すべての指示文に、以下を必ず含めること**：

1. **送信者リストの提示と承認**
   送信を始める前に「送信者リスト」（患者番号／名前／来院日時など）を提示し、ユーザーのOKを待つ。

2. **送信プレビューの確認**（送信者リスト承認後・送信前に必ず）
   送信者リストにOKをもらったら、いきなり送らず、まず「実際に送る文面のプレビュー（テンプレートに
   日付・曜日・時刻・名前を当てはめた完成文／改行・空行・全角数字・絵文字も送る形のまま）」を提示し、
   もう一度ユーザーのOKを待つ。修正指示が出たら直してから再度プレビューを見せる。
   ※プレビューは全員分ではなく、代表として1人分の一例だけでよい。

→ つまり LINE送信は必ず「①送信者リストの承認 → ②文面プレビューの承認 → ③送信」の2段階確認を経ること。
ユーザーの承認なしに勝手に送信を始めない。
