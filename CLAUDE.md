# CLAUDENOTE プロジェクトメモ

博多ステラ歯科の業務支援リポジトリ。

## LINE用の指示文を作るときの必須ルール

今後 **LINE用の指示文**（一斉送信・リマインド等）を作成・更新するときは、**必ず以下を毎回含める**こと。

1. **改行はすべて「Shift＋Enter」で入れる。**
   - LINE Manager では Enter 単独が送信になるため、文面の改行は必ず Shift＋Enter にする旨を明記する。
2. **送信後は必ずその相手の「手動チャット」をオフにする。**
   - 送信とセットで毎回オフにするよう、手順に明記する。

> これらは省略しないこと。新しいLINE指示文を作るたびに上記2点を入れる。
