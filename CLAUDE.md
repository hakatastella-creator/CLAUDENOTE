# CLAUDENOTE 作業ルール

博多ステラ歯科の業務システム置き場。ここのルールは毎回のセッションで必ず守ること。

## 1. システム作成依頼を受けたら、まず「受付フォルダ用か」を確認する

**新しくシステム・ツール・ファイルを作ってほしいと頼まれたら、着手する前に必ず
「これはデスクトップの受付フォルダ用ですか？」と本人に確認すること。**

- 確認せずに作り始めない。依頼内容から自明に見えても必ず一度聞く。
- 「受付フォルダ用です」と言われたら → 下の「受付フォルダ用の要件」を満たす形で作る。
- 「違います」と言われたら → 通常どおり作ってよい。

## 2. 受付フォルダ用の要件：他のパソコンからも変更できること

受付フォルダのシステムは受付スタッフが複数のパソコンから使う。
**1台のパソコンでしか編集・閲覧できない作りにしないこと。**

必須:

- **データをブラウザの localStorage だけに保存しない。**
  localStorage はそのパソコン・そのブラウザの中にしか残らないため、
  別のパソコンで開くと中身が空になる。これが一番やりがちな失敗。
- 入力したデータは、**全員が同じ中身を見られる場所**に保存する。想定する方式:
  1. 受付フォルダ自体が OneDrive 等で同期されている前提で、
     同じフォルダ内の JSON / CSV / xlsx ファイルに読み書きする
     （ファイル選択・ダウンロード保存で運用するなら手順を画面内に明記する）
  2. Google スプレッドシート + GAS Web アプリなどの共有バックエンドに保存する
  - どちらにするかは作る前に本人に確認する。
- **単一の HTML ファイルで開けば動く形を保つ**（Edge でダブルクリックするだけ）。
  インストールやサーバー起動を必要とする作りにしない。
  補助ファイルが必要なら `〇〇_files` フォルダにまとめる（既存システムと同じ流儀）。
- 文字コードは UTF-8、日本語 UI。ファイル名も日本語のまま分かりやすく。

## 3. 現在の受付フォルダの中身（2026-09 時点）

ほとんどが Claude 作成のシステム。改修依頼はこれらに対して来ることが多い。

HTML システム:
- ステラ受付メモ.html / ステラ受付メモ（こっちに入力！）.html（+ `ステラ受付メモ_files`）
- 口コミ管理.html（+ `口コミ管理_files`）
- 月次レポート作成.html
- 月次作成ファイル.html
- 満足度アンケートURL生成ツール.html

Excel:
- プレオルソ　発注患者リスト.xlsx
- 株式会社ステラ売上表（本物）.xlsx
- 給与連絡票_博多ステラ歯科・矯正歯科クリニック.xlsx
- 業者名刺一覧.xlsx
- 自費補綴_値引き価格表.xlsx
- 新現金出納帳.xlsx

その他フォルダ:
- フォレストワン　いびき

## 4. このリポジトリ自体について

`src/` の毎日の返信下書き自動生成（Gmail / Chatwork）は GitHub Actions で動く別系統。
受付フォルダのシステムとは無関係。詳細は `README.md` と `docs/SETUP.md` を参照。
