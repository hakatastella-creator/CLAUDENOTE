# CLAUDENOTE 作業ルール

博多ステラ歯科の業務システム置き場。ここのルールは毎回のセッションで必ず守ること。

## 1. システム作成依頼を受けたら、まず「受付フォルダ用か」を確認する

**新しくシステム・ツール・ファイルを作ってほしいと頼まれたら、着手する前に必ず
「これはデスクトップの受付フォルダ用ですか？」と本人に確認すること。**

- 確認せずに作り始めない。依頼内容から自明に見えても必ず一度聞く。
- 「受付フォルダ用です」と言われたら → 下の「受付フォルダ用の要件」を満たす形で作る。
- 「違います」と言われたら → 通常どおり作ってよい。

## 2. 受付フォルダ用の要件：他のパソコンからも変更できること

受付フォルダのシステムは受付スタッフが複数のパソコンから使う。
**1台のパソコンでしか編集・閲覧できない作りにしないこと。**

### 方式A（第一候補）：Claude Artifact として作り、URL で共有する

既存の主要システム（ステラ受付メモ、口コミプレゼント台帳）はこの方式で、
**すでに他のパソコンから使える状態になっている**。新規もこれに合わせるのが基本。

- データはアーティファクト自身に保存する
  （`window.claude.use("artifact")` → ページ自身のソース内の
  `<script id="..." type="application/json">` を差し替えて `publish`）。
  保存した瞬間に全員の画面へ反映される。
- localStorage は**その端末の控え**としてのみ使い、起動時に埋め込みデータと突き合わせる。
  1件ずつ id と updatedAt で比べる方式にして、2台で同時に入力しても消えないようにする。
  削除は tombstone（`{id, at}`）で表現し、古い控えから復活しないようにする。
- 保存前に最新版を読み直してマージしてから publish する。`conflict` / `not_writer` /
  `rate_limited` などのエラーを個別に扱い、保存できないときは画面にはっきり出す。
- スタッフに渡すのは**アーティファクトの URL**。ファイルではない。

### 方式B：受付フォルダ内のデータファイルに読み書きする

Excel など、どうしてもローカルのファイルでないと困るもの向け（本人決定・2026-09）。

- 受付フォルダは OneDrive で同期されている前提。同じフォルダに置いた
  JSON / CSV / xlsx を実データとし、同期経由で他のパソコンと共有する。
- 実装は File System Access API（`showDirectoryPicker` / `showSaveFilePicker`、Edge で動作）。
  初回に受付フォルダを一度選び、ハンドルを IndexedDB に保存して以後は選び直さずに使う。
- 使えない環境用に、ファイル読み込み + ダウンロード保存の手動フォールバックを必ず用意する。
- 保存時にファイルの更新時刻を確認し、読み込み後に他の人が更新していたら上書きせず警告する。
- 保存前の版を1世代残す。

### どちらの方式でも

- 文字コードは UTF-8、日本語 UI。ファイル名も日本語のまま分かりやすく。
- **データをブラウザの localStorage だけに置いて終わりにしない。**
  localStorage はその端末のその ブラウザにしか残らない。

## 3. 最重要：デスクトップの .html は「保存したスナップショット」

**受付フォルダにある `ステラ受付メモ.html` `口コミ管理.html` は、システム本体ではない。**
Edge の「名前を付けて保存」で claude.ai のアーティファクト画面を保存したもので、
中身は claude.ai の画面の写しと `〇〇_files` フォルダ。

これをダブルクリックして開くと:

- `window.claude` が存在しないため保存機能が働かず、画面には「この端末のみ」と出る
- 入力はその パソコンの localStorage に溜まるだけで、**他のスタッフには一生届かない**
- 表示される内容も保存した日のまま止まっていて、その後の更新は反映されない

**対処：ファイルではなく URL を開くようスタッフに徹底する。**
受付フォルダには .html のスナップショットではなく、URL のショートカットを置く。
（`〇〇.html` という名前で保存されていると本物に見えてしまうのが厄介。）

## 4. 現在の受付フォルダの中身（2026-09 時点）と点検結果

点検日 2026-09-13。本人からアップロードしてもらった5点を確認した。

| デスクトップのファイル | 正体 | 状態 |
|---|---|---|
| ステラ受付メモ.html | Artifact `d1988dc7-262c-4e13-8760-3b1363e1b44a` のスナップショット | ✕ 開くと1台限定。本体は URL |
| ステラ受付メモ（こっちに入力！）.html | 上と**バイト単位で同一**の重複コピー | ✕ 同上。区別する意味がない |
| 口コミ管理.html | Artifact `5bc2d82d-5110-4361-9277-e970f40431d9`（口コミプレゼント台帳）のスナップショット | ✕ 同上 |
| 満足度アンケートURL生成ツール.html | 単体で完結した本物の HTML ツール | △ 動作は問題なし。下記参照 |
| プレオルソ　発注患者リスト.xlsx | 通常の Excel（発注リスト／マスタの2シート） | ○ OneDrive 同期で他PCから編集可 |

未確認: 月次レポート作成.html / 月次作成ファイル.html / 口コミ管理_files /
フォレストワン　いびき / その他の xlsx。

**満足度アンケートURL生成ツール.html について**
- 受付番号の変換は `ST` + (カルテ番号 × 373 + 502847 の下6桁)。
  **373 と 502847 は絶対に変更しない**（変えると過去に発行した番号が逆引きできなくなる）。
- 患者リストは毎回貼り付ける運用なので、データが端末に閉じ込められる問題はない。
- ただし **LINE 送信文面と送信先フォームの設定が localStorage 保存**なので、
  文面を直してもそのパソコンにしか残らない。他のパソコンでは既定の文面に戻る。

## 5. 受付フォルダのシステム一覧（アーティファクトの URL）

スタッフに渡すのはこの URL。デスクトップの .html ではない。

| システム | URL |
|---|---|
| ステラ受付メモ | https://claude.ai/code/artifact/d1988dc7-262c-4e13-8760-3b1363e1b44a |
| 口コミプレゼント台帳 | https://claude.ai/code/artifact/5bc2d82d-5110-4361-9277-e970f40431d9 |
| インプラント予定患者表 | https://claude.ai/code/artifact/1609d674-2148-4b99-bb5e-dd2d589a1416 |

### インプラント予定患者表（2026-09-13 作成）

オステムへの送付から埋入までを7工程で追い、患者ごとに「次にやること」と期限を出す。
作った目的は**予約連絡の漏れをなくすこと**なので、藤垣先生の確認OKが入った瞬間に
「患者へ予約連絡」が最優先で立ち上がる設計になっている。ここを弱めないこと。

工程（10段階）: ①資料送付 ②承認依頼（修正依頼あり） ③藤垣先生OK ④予約連絡
⑤ガイド試適 ⑥埋入 ／ ⑦2次オペ ⑧型取り ⑨発注 ⑩セット

⑦以降が被せ（アバットメント・クラウン）の工程。2026-09-13 に追加した。
工程は「いちばん先まで日付が入っているところの次」を見る方式（`SEQ` 配列）なので、
既に進んでいる患者を途中から登録しても、正しい段階に飛ぶ。

**「レポート到着」の工程は本人の指示で外した（2026-09-13）。勝手に戻さないこと。**
レポートが届いた日は記録せず、送付日＋中10日を「オステムへ返信する期限」として使う。
修正依頼を出したあとは期限をやめ、修正依頼日からの経過日数で追う。

本人の説明から置いた前提（変えるときは必ず確認する）:

- **中10日 = 送付日の11日後**（`GUIDE_DAYS = 11`）。オステムへ返信する期限に使う。
- **埋入 = ガイド試適の4〜5日後**（`PLACE_MIN/MAX = 4/5`）。目安表示のみで、予約日は手入力。
- 藤垣先生の確認と修正版レポートは**所要日数が不明なので期限を作っていない**。
  代わりに経過日数を出し、7日（`STALE_DAYS`）を超えたら知らせる。
  実際の日数が分かったら期限に変えてよい。
- 予約連絡の期限は**藤垣先生OKの当日**。翌日から「◯日 遅れ」になる。
- **⑦以降の患者連絡はすべて LINE**（本人指定）。連絡日と予約日を分けて持ち、
  連絡していなければ「LINEで連絡して◯◯の予約を取る」が赤く立ち上がる。
- **2次オペは埋入から3〜6ヶ月**（`OP2_MIN_M`/`OP2_MAX_M`）。患者によって違うので
  期限ではなく「時期の幅」で扱う。3ヶ月で「連絡の時期」、6ヶ月を超えて初めて遅れ。
- **アバットメント・クラウンの納品は15営業日**（`DELIVER_BD`、土日祝を除く）。
  祝日は `holidaysOf()` で計算している（ハッピーマンデー・春分秋分・振替休日・国民の休日）。
  **オステムの年末年始やお盆の休みは入っていない**ので、その時期はずれる。
- **2次オペから型取りまでの治癒待ちは暫定28日**（`HEAL_DAYS`）。
  実際の日数は本人に確認できていない。分かったら直すこと。
- **仮歯（プロビジョナル）は未実装。「後で設定したい」とのこと（2026-09-13）。**
  入れる場合は⑧型取りの前後のどこかになる。作る前に位置を確認すること。
- 絞り込みは **全部／要対応／終了 の3つ、並びもこの順、既定は「全部」**（本人指定・2026-09-13）。
  以前あった「進行中すべて」は削除済み。

**改修するときは、必ず先にライブ版のデータを取り込むこと。**
このアーティファクトは受付が日々入力している。手元のソースを
そのまま publish すると患者データを消す。手順は
`read_file`（path: `index.html`）でライブ版を保存 →
`<script id="implant-data">` の中身を手元のファイルへ移す → publish。
版が進んでいると publish は拒否されるので、`read` で最新を確認してからやり直す。

## 6. このリポジトリ自体について

`src/` の毎日の返信下書き自動生成（Gmail / Chatwork）は GitHub Actions で動く別系統。
受付フォルダのシステムとは無関係。詳細は `README.md` と `docs/SETUP.md` を参照。
